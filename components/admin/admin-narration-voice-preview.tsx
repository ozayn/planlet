"use client";

import { NarrationVoicePreviewMatrix } from "@/components/settings/narration-voice-preview-matrix";

/** Admin page reuses the Settings voice preview matrix. */
export function AdminNarrationVoicePreview() {
  return <NarrationVoicePreviewMatrix />;
}
